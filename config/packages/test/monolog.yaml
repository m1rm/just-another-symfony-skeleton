monolog:
    channels: ['weather']
    handlers:
        weather:
            type: stream
            path: '%kernel.logs_dir%/weather.log'
            level: debug
            channels: ['weather']
